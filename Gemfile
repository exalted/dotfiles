source "https://rubygems.org"

# Ruby language server for the editor. Zed's Ruby extension runs
# `bundle exec solargraph`, so solargraph must live in this project's bundle
# for Ruby language support to work when editing files in this repo.
gem "solargraph", require: false
