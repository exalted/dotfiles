# export EDITOR=emacs

# Single quotes around double quotes is necessary, or otherwise Claude code won't be able to open the editor, for example when using `/memory` command:
export VISUAL='"/Applications/Zed Preview.app/Contents/MacOS/cli"'
