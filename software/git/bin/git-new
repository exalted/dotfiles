#!/usr/bin/env bash
set -euo pipefail
trap "echo 'error: Script failed: see failed command above'" ERR

# TODO: try to update master from origin/master first, but don't fail if cannot
$GIT checkout -b $(kebab "$@") --no-track
