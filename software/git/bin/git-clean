#!/usr/bin/env bash

args=()

# Make sure to not remove local Claude code settings errorneously:
if [ -d ".claude/" ]; then
    args+=("--exclude=/$(git rev-parse --show-prefix).claude/")
fi

git clean "${args[@]}" "$@"
